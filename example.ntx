# Sample natrix source code

# Currently, natrix programs are just simple expressions, one per line, with no leading whitespaces
# See doc/grammar.md

# The result of each expression is printed to the standard output

(10 - 3) * 6  # comments are ignored
1 + 48 / 4
